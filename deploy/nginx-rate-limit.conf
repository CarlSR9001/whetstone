# This zone is inert outside the Whetstone vhost. The Python service applies a
# second limiter and a stricter per-IP counterexample-search budget.
limit_req_zone $binary_remote_addr zone=whetstone_api:10m rate=30r/m;
